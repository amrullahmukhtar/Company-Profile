/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['storage.googleapis.com', 'asset.kompas.com', 'images.pexels.com'],
    },
  };
  
  export default nextConfig;

